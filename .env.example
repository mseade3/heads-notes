# Public template — copy to `.env.local` (never commit secrets).

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
OPENAI_API_KEY=

# Optional allowlists (comma-separated emails / single domain)
ALLOWED_CORE_EMAILS=
ALLOWED_EMAIL_DOMAIN=

# Optional: model override for `npm run eval -- --live`
# EVAL_MODEL=gpt-4.1
